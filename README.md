# Blog Platform — React + TypeScript + CSS Modules

Клиент блога, совместимый с RealWorld (Conduit) API: лента статей, просмотр с поддержкой Markdown, лайки, создание/редактирование/удаление статей, авторизация и редактирование профиля.

---

## ✨ Возможности

    •	Авторизация: регистрация, вход/выход; токен хранится в localStorage.
    •	Профиль: изменение имени, e-mail, пароля и необязательного аватара (URL).
    •	Лента статей с пагинацией (по 5 на страницу).
    •	Страница статьи: отображение Markdown (react-markdown), теги, автор, дата (форматирование через date-fns).
    •	Лайк/анлайк со счётчиком и мгновенным обновлением.
    •	CRUD статей: создание, редактирование и удаление (только автор); подтверждение действий через Popconfirm.
    •	Состояния загрузки и ошибок: спиннер и информирование об ошибках.
    •	Производительность: мемо-селекторы (reselect).
    •	Валидация форм: react-hook-form.

## 🧰 Стек

    •	React 18, TypeScript
    •	React Router v6
    •	Redux Toolkit, react-redux, redux-thunk
    •	react-hook-form, date-fns, react-markdown, reselect
    •	CSS Modules
    •	Create React App (react-scripts@5)
    •	ESLint (Airbnb + TypeScript) и Prettier
    •	Архитектура Feature-Sliced Design (FSD)

---

## 🧱 Архитектура (FSD)

    •	app — инициализация приложения, роутер, провайдеры, store.
    •	pages — отдельные страницы (список статей, статья, логин, регистрация, профиль и т.д.).
    •	widgets — крупные UI-блоки, собирающие данные из нескольких сущностей (например, Header).
    •	entities — доменные сущности (Article и др.).
    •	features — прикладные фичи (лайк статьи, авторизация и т.п.).
    •	shared — переиспользуемые вещи: UI-компоненты, утилиты, типы, стили, ассеты.

---

## 🚀 Демо

Развёрнуто на Vercel:  
**<https://blog-platform-beige-phi.vercel.app/>**

---

## 🛠 Как запустить

```bash
# установка
npm i

# dev-сервер
npm start

# сборка
npm run build
```
